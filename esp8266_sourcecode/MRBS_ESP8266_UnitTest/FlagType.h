#ifndef __FLAG_TYPE_H__
#define __FLAG_TYPE_H__

typedef enum FlagButtonAPMode 
{
    IN_AP_MODE = 0,
    NOT_IN_AP_MODE,
    TURN_ON_AP_MODE,
    TURN_OFF_AP_MODE,
} FlagButtonAPMode_t;

typedef enum FlagButtonBreaktime
{
    IN_BREAKTIME_MODE = 0,
    NOT_IN_BREAKTIME_MODE,
} FlagButtonBreaktime_t;

#endif